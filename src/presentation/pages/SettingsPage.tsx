import { SectionPlaceholder } from '../components/SectionPlaceholder'

export const SettingsPage = () => {
  return (
    <SectionPlaceholder
      subtitle="Settings"
      title="Application settings"
      description="Theme preferences, Groq configuration, and local data import/export will be wired up in later slices."
    />
  )
}
