import { SectionPlaceholder } from '../components/SectionPlaceholder'

export const ProjectsPage = () => {
  return (
    <SectionPlaceholder
      subtitle="Projects"
      title="Projects index"
      description="The projects workspace will later host CRUD, project planning, and project-level navigation."
    />
  )
}
