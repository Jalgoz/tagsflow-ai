import { SectionPlaceholder } from '../components/SectionPlaceholder'

export const TasksPage = () => {
  return (
    <SectionPlaceholder
      subtitle="Tasks"
      title="Global tasks view"
      description="The global tasks table and editing flows will arrive in a later implementation slice."
    />
  )
}
